{% extends 'SoundGapContentAdminBundle:Layout:ca.html.twig' %}

{% block title %}{{pageName|capitalize}}{{parent()}}{% endblock %}


{% block content %}
<div class='col-sm-7'>
  <h2>{{pageName|capitalize}} List</h2>
  <table class="table">
  <thead>
    <tr>
      <th>Pose</th>
      <th>Character</th>
      <th>Image</th>
      <th>Actions</th>
    </tr>
  </thead>
  {% for characterPose in characterPoses %}
    <tr>
      <td>{{characterPose.pose}}</td>
      <td>{{characterPose.character}}</td>
      <td><img class='img-thumbnail' src="/{{characterPose.image.webPath}}"></td>
      <td>
        <div>
          <a class='label label-warning' href="{{path('soundgap_ca_parts_characterPose_update',{'id':characterPose.id})}}"> update </a>
        </div>
        <div>
          <a class='label label-danger' href="{{path('soundgap_ca_parts_characterPose_delete',{'id':characterPose.id})}}" onclick="return confirm('sure bo?')"> delete </a>
        </div>
      </td>
    </tr>
  {% endfor %}
  </table>
</div>

<div class='col-sm-5'>
<h2>{{actionName|humanize}} {{pageName|humanize}}</h2>
{{ form_start(form, {'method': 'POST','attr':{'class':'form-horizontal','role':'form'}}) }}
{{ form_end(form) }}
</div>
{% endblock %}